@using (Form())
{
    @using (Panel(headingText: "Control Library Test : Modal2", panelLayout: PanelLayout.Stacked, modalStyle: ModalStyle.Small))
    {
        @Button(transition: "Back")
        @Button(transition: "Close")
    }
}