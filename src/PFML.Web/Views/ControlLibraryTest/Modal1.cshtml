@using (Form())
{
    @using (Panel(headingText: "Control Library Test : Modal1", panelLayout: PanelLayout.Stacked, modalStyle: ModalStyle.Small))
    {
        @Button(transition: "Back")
        @Button(transition: "Modal")
    }
}